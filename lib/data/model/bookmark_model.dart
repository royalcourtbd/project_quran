class BookmarkModel {
  String iconPath;
  String title;
  String subTitle;
  String optionIconPath;

  BookmarkModel({
    required this.iconPath,
    required this.title,
    required this.subTitle,
    required this.optionIconPath,
  });
}
